{
  "_comment": "MG-23 blocker 1 fixture — MUST PASS (exit 0). A converged no-op plan: resource_changes is a VALID EMPTY ARRAY. This is the shape `terraform plan` emits when nothing needs to change, which is exactly what the final drift plan in infra-apply-dev.yml produces on a healthy reconciliation. The fail-closed TYPE validation must NOT reject it — rejecting an empty array would wedge the GitOps loop on every successful run. The App Insights resource below is present with local auth disabled so this fixture also proves the gate genuinely WALKED a resource universe rather than passing vacuously.",
  "format_version": "1.2",
  "terraform_version": "1.9.0",
  "planned_values": {
    "root_module": {
      "resources": [
        {
          "address": "azurerm_application_insights.main",
          "type": "azurerm_application_insights",
          "name": "main",
          "values": {
            "local_authentication_enabled": false,
            "instrumentation_key": "11111111-1111-1111-1111-111111111111",
            "connection_string": "InstrumentationKey=11111111-1111-1111-1111-111111111111;IngestionEndpoint=https://westus2-0.in.applicationinsights.azure.com/"
          }
        }
      ]
    }
  },
  "resource_changes": []
}
