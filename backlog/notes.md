SESSION 2026-07-22 — THREE operational reviews addressed. main GREEN (bf41d0e). Paused at live/operator boundary.

3rd REVIEW (5 findings) — ALL addressed:
- finding3 main RED (SIGPIPE in tf-static-checks.sh: echo|grep-q under pipefail, nondeterministic on Ubuntu): FIXED — all echo|grep→here-strings, PR#19 merged f4a60a0. Authored DIRECTLY by me (agent OAuth was down) as emergency; PASSED post-hoc red-wide. LESSON: watch POST-MERGE main CI, not just PR CI (memory saved) — PR-green≠main-green is how main went red unnoticed.
- finding4 MG-32 bootstrap.test.sh 127-crash on macOS Bash 3.2 (functions inside command substitution): FIXED — moved mocks to top level, verified 107/107 on /bin/bash 3.2.57, PR#20 merged c9799bf. MG-32 RE-CLOSED (all AC walked; F5 now runs in operator env).
- findings1/2/5 native-OTLP was WRONG (not just deferred): CORRECTED to MS-documented shape via research vs primary sources (run run-native-otlp-exact-facts-research-0dd49c). PR#21 merged bf41d0e: image 0.151.0@sha256 (clears >=0.132/>=0.148/>0.150 GHSA-pjv4-3c63-699f advisory); azureauth→azure_auth + explicit scopes:[https://monitor.azure.com/.default]; exporter traces_endpoint=.../dataCollectionRules/<immutable-id>/streams/Microsoft-OTLP-Traces/otlp/v1/traces (x-ms-* headers DELETED); DCR→azapi_resource Microsoft.Insights/dataCollectionRules@2024-03-11 (references.applicationInsights+directDataSources.otelTraces+Microsoft-OTel-Traces-* streams+enrichment); azapi provider added. red-wide PASS, terraform validate + otelcol validate green. DOC-VERIFIED but authored+static-only, NOT operational.

1st+2nd review (earlier this session): F1 bootstrap HCL, F2/F3/F4/F5/F6 — all addressed; MG-32 (bootstrap) + MG-33 F2 trace-continuity merged.

MG-33 STAYS OPEN — blocked by MG-34: secure off-VNet edge ingress (Pi→collector: mTLS/token/private-link, NO public unauth listener) + live Go-span-to-App-Insights proof + negative RBAC check (remove MMP-on-DCR→ingestion rejected). MG-24/MG-25-gated. Native-OTLP is fail-closed (no CApp ingress+loopback receiver) + default-off (enable_native_otlp) until then.

REMAINING all operator/live/decision-gated: MG-24 bootstrap keystone; MG-25 native-OTLP preview acceptance; MG-34 live proof+edge ingress; MG-6 Sentry decision; MG-30 device-ownership; MG-31 payload; MG-14 AC5; MG-29; MG-13/MG-7 Phase3.
SAFETY: V2 greenfield — never touch V1; no local-state apply; local_authentication_enabled + enable_native_otlp STAY false; no manual Azure resources; PROD_DEPLOY_ENABLED unset until MG-25.
OPS/LESSONS: (1) WATCH post-merge main CI every merge/direct-push (memory). (2) Completion Monitors use 'forge show <run-id>' run-status, not 'forge launch show' grep (command-echo false-fires) (memory). (3) infra red-wide catches operational defects terraform validate misses — run it (memory). (4) forge review-loop hangs (nx) → red-wide+green CI. (5) agent OAuth can expire mid-session (all invokes fail 'session expired'); operator re-auth needed; recovered on its own this time. (6) native-OTLP vendor semantics: research primary MS sources, don't author from memory. (7) gh pr merge --delete-branch switches local to main. (8) Local npm 11.13 vs pinned 10.9.8 — never regen lockfile locally.
