SESSION 2026-07-22 — FOUR operational reviews all addressed at AUTHORED level. main GREEN (c67eeca). Paused at live/operator boundary.

MG-33 (native-OTLP corrections) is AUTHORED-COMPLETE across all 4 reviews; blocked ONLY by MG-34 for closure.
- Review 1/2 (F1-F6): bootstrap HCL/tests/fail-loud (MG-32, closed) + Go OTLP→collector + per-reading trace + docs.
- Review 2: F2 trace-continuity (poll trace carried end-to-end, #16); F1 collector-can't-auth → operator chose native OTLP.
- Review 3: main-red SIGPIPE (tf-static-checks echo|grep→here-strings, #19, authored directly during agent-OAuth outage, passed post-hoc red-wide); MG-32 bash-3.2 test (#20, verified 107/107 on macOS 3.2, MG-32 reclosed); native-OTLP was WRONG → corrected to MS-documented shape via primary-source research (#21): image 0.151.0@sha256:d57b... (clears GHSA-pjv4-3c63-699f advisory + version floors), azure_auth + explicit scopes:[https://monitor.azure.com/.default], traces_endpoint (.../streams/Microsoft-OTLP-Traces/otlp/v1/traces, NOT x-ms headers), azapi Microsoft.Insights/dataCollectionRules@2024-03-11 (references.applicationInsights + directDataSources.otelTraces + Microsoft-OTel-Traces-* + enrichment).
- Review 4 (#22, merged c67eeca): #1 tables OTelSpans/OTelTraces everywhere (MG-34 AC2 + README + ADR + variables.tf + collector-config.yaml — native OTLP lands in OTel-schema tables, NOT AppTraces/AppDependencies); #2 removed orphan Microsoft-OTel-Logs from DCR dataFlows (traces-only, no otelLogs data source — would fail Azure DCR validation at apply); #3 CI otelcol image pinned to exact tag+digest.

MG-33 native-OTLP is DOC-VERIFIED (MS primary sources) but operationally UNPROVEN until MG-34 live proof. fail-closed (no CApp ingress + loopback receiver) + default-off (enable_native_otlp) until then.

MG-34 (blocks MG-33 closure): AC1 secure off-VNet edge ingress (Pi→collector: mTLS/token/private-link, NO public unauth listener); AC2 live Go-span-to-App-Insights proof — queryable by TraceId in OTelSpans/OTelTraces + surfaces in App Insights experience; AC3 negative RBAC check (remove MMP-on-DCR → ingestion rejected). MG-24/MG-25-gated.

REMAINING all operator/live/decision-gated: MG-24 bootstrap keystone; MG-25 native-OTLP preview acceptance; MG-34 live proof+edge ingress; MG-6 Sentry decision; MG-30 device-ownership; MG-31 payload; MG-14 AC5; MG-29; MG-13/MG-7 Phase3. Nothing else offline-closeable.
SAFETY: V2 greenfield — never touch V1; no local-state apply; local_authentication_enabled + enable_native_otlp STAY false; no manual Azure resources; PROD_DEPLOY_ENABLED unset until MG-25.
OPS/LESSONS (memories saved): watch POST-MERGE main CI every merge/direct-push (PR-green≠main-green); completion Monitors use 'forge show <run-id>' run-status not 'forge launch show' grep (command-echo false-fires); infra red-wide catches operational defects terraform validate misses; native-OTLP/vendor semantics — research MS primary sources, don't author from memory; agent OAuth can expire mid-session (recovered on its own); gh pr merge --delete-branch switches local to main; red-wide catches doc/prose drift one file at a time — do repo-wide greps.
