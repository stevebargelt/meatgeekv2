**Session 2026-07-20.**

**AZURE FACTS (authoritative):** V2 is GREENFIELD — no V2 Azure infra exists (`meatgeek-dev-rg` deleted); all remaining MeatGeek Azure resources are V1, OUT OF SCOPE (never import/touch/modify). No V2 migration, no state to recover. HARD SAFETY: never touch/import V1; no terraform apply against local/ephemeral state; no manual Azure resource creation to satisfy proofs; no prod deploy enabled.

**CRITICAL PATH — where we are:**

1. **MG-24 (greenfield V2 infra) — DETERMINISTIC LAYER MERGED (main `21be588`, PR #4). STILL OPEN.** Shipped + reviewed (6 red-wide rounds + green CI): remote-state backend + OIDC identity bootstrap; per-env isolated state (`meatgeek-v2/dev.tfstate`, `prod.tfstate`); no hardcoded subscription id; V2 naming `meatgeek-v2-${env}-*`; V2 OWNS its Cosmos; no `timestamp()` drift; FA-name single source; **managed identity across all services, NO avoidable secrets in state** (Cosmos/Storage/IoT-Hub/SignalR identity-based; App Insights via AAD/Monitoring-Metrics-Publisher, endpoint-only conn string); **Easy-Auth default-deny** (require auth on all endpoints, no anonymous business/health); wildcard-CORS removed (per-env origins); static V1-safety gate (FAILS on local *.tfstate); dev+prod SEPARATE OIDC federated identities (subjects `repo:<owner>/<repo>:environment:development|production`); CI plan-only. Accepted residual (ADR `learnings/decisions/mg-24-appinsights-key-in-terraform-state.md`): the AI resource's own computed key attribute is inherently in state — accepted (telemetry-write-only, AAD auth, restricted state).
   **REMAINING (operator, out-of-band) → the ONLY thing gating everything downstream:** run the greenfield bootstrap + dev `plan`/`apply` per `docs/infrastructure/bootstrap-runbook.md`: bootstrap state-storage + OIDC identity → `terraform plan` (empty dev state) → HUMAN review (scope/security/cost) → `apply` (creates V2 dev stack incl. Function App) → 2nd plan no-op → representative change → incremental plan/apply → no-op. Capture state key + plan/apply evidence + resource inventory. This closes MG-24 AND creates the dev Function App MG-21 needs.

2. **MG-21 (prod deploy corrective) — OPEN, blocked on MG-24.** Config MERGED (`3dd4165`, workflow_run CI-gated + PROD_DEPLOY_ENABLED + TOCTOU-safe stale-SHA guard + managed-identity Functions package + valid package). Closing evidence = deploy commit `3dd4165`'s package to the **MG-24-created dev Function App** (dev creds), AUTHENTICATED smoke test, capture invocation log. Do NOT manually create a Function App. `PROD_DEPLOY_ENABLED` stays DISABLED. Checklist on ticket.

3. **MG-23 (dev deploy split) — OPEN, depends on MG-24.** infra-deploy-dev plan-only until MG-24; then reconcile TF-owned dev stack via persistent dev state; app-deploy-dev targets the IaC FA name/identity; remove deploy-dev + orphaned artifact upload from ci.yml.

4. **MG-25 (prod activation) — OPEN, greenfield, depends on MG-24 + security.** Create prod from empty prod state via a reviewed creation plan; add prod-env secret; set `PROD_DEPLOY_ENABLED=true` LAST.

5. **Feature work (after infra):** MG-9 (data-models, cleanest start), MG-14 (Phase 2 SignalR), MG-6 (OTel).

**Shipped this session:** MG-20 (npm pin) + MG-22 (nx-set-shas) — PR #1 `56b0038`. MG-21 first split (reopened) — PR #2 `dc5df4d`. MG-21 corrective — PR #3 `3dd4165`. MG-24 greenfield deterministic layer — PR #4 `21be588`.

**External state:** GitHub PUBLIC; branch protection on `main` (setup, lint-and-test x4, build-typescript x2, build-go x2, validate-infrastructure, security-scan; deploy EXCLUDED). Prod creds ABSENT + PROD_DEPLOY_ENABLED unset (app-deploy-prod skips). Prod API-only.

**Ops notes:** (1) `forge review-loop` HANGS on host-side local verification here (nx isolation plugin-worker) — use `forge invoke red-wide` (container) + green PR CI as the review gate. (2) Use **Monitor** on launched work for prompt completion — don't rely on fixed ScheduleWakeup ticks. (3) The feature pipeline's build task goes 'complete' + can't be gated back; drive fix rounds via direct engineer + red-wide instead.

**Decisions not to relitigate:** V2 GREENFIELD; MG-24 keeps App Insights TF-managed with the accepted key-in-state residual (ADR); MG-21 corrective = workflow_run CI-gating + env-secret + PROD_DEPLOY_ENABLED (not a guard job); dev proof (authenticated) closes MG-21; prod activation = MG-25 (enable var LAST). Interim durable-dispatch rule (forge launch run + Monitor) until FG-552/562/563.

**Older shipped:** MG-15/16/17/18/19.

**MG-24 CORRECTIVE LAYER MERGED — main 4df8427 (PR#5, squash), 2026-07-21.** After the operational review reopened MG-24, the deterministic corrective layer went through 23 red-wide rounds (each fixing a real defect until round-23 PASS, 0 findings) + green CI, then merged. Fixes landed: Supabase purged from operator setup doc (docs reconciled to shipped Entra Easy Auth; unimplemented app-layer Supabase design quarantined behind a status banner — full reconciliation = MG-26, filed/deferred); secret gate tf-plan-secret-inspection.sh made fully fail-closed (every jq collector — resource universe, app_settings/outputs sinks, App Insights count + local-auth-disabled + ai_ikeys — dies on jq failure instead of || true-swallowing into a false PASS); Easy Auth hardened to bearer-validation-only (www_authentication_disabled=true, unauthenticated_action=Return401, no client secret, token_store_enabled=false) with FOUR fail-closed preconditions requiring client_id + tenant_id + allowed_audiences + non-empty allowed_client_app_ids together; bootstrap.sh made fully idempotent (all 4 az role assignment creates guarded create-if-absent via ensure_role_assignment helper; BOTH OIDC federated credentials reconcile their SUBJECT via a shared ensure_federated_credential helper — create/no-op/delete+recreate on drift). Verified operationally each round: api-interfaces gate spec 88/88, terraform validate clean, gate dies on malformed JSON / passes on valid empty plan, bash -n clean.

**MG-24 STILL OPEN.** REMAINING AC = the operator out-of-band greenfield bootstrap + dev plan/apply/publish/AUTHENTICATED-smoke (10-step sequence in the recap / bootstrap-runbook). This is the ONLY thing gating MG-21 (needs the dev Function App), MG-23, MG-25. Do NOT run the live bootstrap for the operator (no Azure session; destructive/cost). NO V1 touch; no local-state apply; no manual Azure resource creation; PROD_DEPLOY_ENABLED stays unset.

**Now working: MG-9** (data-models bug fixes, ready characterization-test AC) — the only unblocked substantial ticket. MG-26 (authentication.md app-layer docs) also open/deferred.

**MG-9 SHIPPED + CLOSED — main 0ebf184 (PR#6), 2026-07-21.** Unified cook-manager/DataValidator on @meatgeekv2/utils single source of truth (new resolveMeatType + VALIDATION.WEIGHT); fixed cookId entropy (randomUUID), calculateRSquared + estimateCompletionTime slope zero-denominator crash, configurable anomaly thresholds, cook-name trim, weight bounds, validateCookNameUniqueness (cook.id). tech-lead plan -> engineer -> test-engineer (26 integration tests) -> 2 red-wide rounds (r1 caught the slope-crash blocker, r2 PASS) -> CI green. 8/8 AC walked with evidence. Docs-impact not-needed (utils API not a documented contract). MG-27 filed (2 out-of-scope pre-existing lows from r1: ADC=0 anomaly characterization bug + createCook undefined-name passthrough). **Now working: MG-27** (small cleanup, full context) then MG-14 (Phase-2 SignalR) / MG-6 (OTel) per feature priority. MG-24 stays OPEN for operator live bootstrap (only gate on MG-21/23/25).

**MG-27 SHIPPED + CLOSED — main 5e29284 (PR#7), 2026-07-21.** ADC=0 routed through convertAdcToTemperature boundary path (all 5 probes explicit presence checks; spy-backed integration test pins it, empirically fails vs truthy gate) + createCook undefined-name fix (request.name.trim(); StartCookRequest.name is required). 2 red-wide rounds (r1 caught toothless test, r2 caught whitespace-empty-name). data-models 259/259. **MG-28 filed** (createCook whitespace-only name emits empty string — fail-safe: createCook NOT wired into live API yet (start-cook.ts is a mock that inlines Cook creation) and validateCook rejects empty/whitespace names; needs a small design decision: constructor-validates vs trusts-validated-input). MG-9 re-closed (closure was lost in a local/remote main reconcile). **Now picking next:** MG-14 (Phase-2 SignalR) / MG-6 (OTel) — may be gated on MG-24 operator Azure bootstrap for live parts; or MG-26/MG-28 (offline). MG-24 still OPEN for operator live bootstrap (gates MG-21/23/25).

**MG-14 AC1-4 MERGED — main b291685 (PR#8), 2026-07-21. MG-14 STAYS OPEN (AC5 blocked on MG-24).** Azure Functions API is now a SignalR PRODUCER: negotiate endpoint (signalRConnectionInfo input binding, group userId=deviceId, 400 on missing deviceId), cook_started publisher on start-cook, new stop-cook fn (POST cooks/{cookId}/stop) cook_stopped publisher, auth via platform-layer MG-24 Easy Auth (authLevel anonymous by design). Cross-language contract locked against the Go data-pusher consumer — red-wide caught 5 real defects over 4 rounds (cook_stopped must OMIT top-level cookId; schema-valid stop payload; cooks.yaml + openapi.yaml + /negotiate path reconciled; unreachable stop 404 dropped). 40 api tests (incl 19 integration mocking SignalR bindings). AC5 (live E2E smoke) = docs/api/signalr-cook-events-smoke.md, BLOCKED-on-MG-24 greenfield SignalR bootstrap (operator step). Follow-ups: MG-29 (data-pusher negotiate handshake protocol, resolve at AC5), MG-30 (bind device-group to authenticated identity vs deviceId query), MG-31 (persist real cook OR minimize cook_stopped payload; stop 404 with persistence). MG-14 docs-impact (diagrams.md stale re producer) being reconciled. **Next: MG-26** (authentication.md Supabase->Entra docs, offline+closeable) then MG-6 (OTel offline slice).

**MG-26 CLOSED — main 340d77d (PR#9), 2026-07-21.** docs/api/authentication.md reconciled to shipped platform-layer Entra Easy Auth (require_authentication, Return401, access_as_user, allowed_applications, no client secret — per main.tf + bootstrap.sh); unimplemented Supabase app-layer design REMOVED (only negations remain); docs/ swept; red-wide PASS. **Next: MG-6 (OTel) offline slice** via tech-lead plan (implementation_full) — OTel Go SDK + Azure Monitor exporter (device-controller/data-pusher), @azure/monitor-opentelemetry (api), W3C trace-context propagation, custom dimensions, observability.md amendment; Sentry org/project creation + DSN = OPERATOR/product decision (STOP+surface); 5 live Azure Monitor alerts = live-infra-blocked. Remaining offline-ish: MG-28 (createCook whitespace, constructor-design decision), MG-30 (negotiate device-group authz — needs device-ownership model, design-heavy), MG-31 (cook_stopped payload persist-or-minimize, design), MG-29 (data-pusher negotiate proto, Go, ties to AC5).

**MG-6 (OTel) STARTED — branch build/mg-6-otel-offline.** Tech-lead scoped 3 buckets. BUCKET A (implementing now, offline): A1 data-pusher Go Azure Monitor exporter, A2 device-controller Go OTel+exporter, A3 api @azure/monitor-opentelemetry+0.5 sampling (adds dep), A4 api correlation/W3C-traceparent helper, A5 observability.md. BUCKET B (OPERATOR DECISION SURFACED via decision_needed milestone): create Sentry org + choose project structure (one-project-plus-envs VS one-per-app) — everything else in B cascades. BUCKET C (blocked-on-MG-24): 5 live Azure Monitor alerts + E2E trace smoke + live IoT-Hub receiver wire-up. MG-6 will stay OPEN (only Bucket A merges). Go Azure Monitor exporter not GA -> engineer chooses backend (Azure Monitor module vs OTLP->collector), not operator-gated.

**MG-28 CLOSED — main 7a03aa6 (PR#11), 2026-07-21.** No invalid cook name can be emitted: createCook throws on empty/whitespace-only name (factory, fail-fast) AND the live start-cook handler (mock, inlines) rejects whitespace-only names with 400 before any cook_started emit + stores the trimmed name. Red-wide r1 caught that fixing only createCook left the live start-cook path broken (createCook not wired live). data-models 285, api 56. docs_impact not_needed (OpenAPI StartCookRequest already requires name; tightened enforcement, no contract change). **Remaining follow-ups: MG-29** (data-pusher SignalR client adapt to API negotiate handshake — Go, offline-implementable, ties to AC5 live), **MG-31** (cook_stopped payload persist-or-minimize — design), **MG-30** (negotiate device-group authz — likely needs a user->device ownership model that does not exist = new scope, surface to operator). Assessing next / possible natural pause.

**SESSION PAUSE 2026-07-21 — all offline-closeable work shipped; remaining is operator-gated.** Shipped this session (7): MG-24 corrective (merged, MG-24 stays OPEN for operator bootstrap), MG-9 (closed), MG-27 (closed), MG-14 AC1-4 (merged, OPEN for AC5), MG-26 (closed), MG-6 Bucket A (merged, OPEN for B/C), MG-28 (closed). OPERATOR-GATED / WAITING: (1) MG-24 greenfield Azure bootstrap [10-step; the keystone — gates MG-21/23/25 + MG-14 AC5 live smoke + MG-6 Bucket C alerts/E2E]; (2) MG-6 Bucket B Sentry project-structure decision [milestone mg-6-sentry-decision]; (3) MG-30 device-ownership-model decision [milestone mg-30-ownership]; (4) MG-31 cook_stopped payload persist-vs-minimize decision [milestone mg-31-payload, recommend minimize]; (5) MG-29 data-pusher negotiate handshake [resolve at MG-14 AC5 live-smoke]. Next session: once operator answers MG-31 (likely minimize) that is offline-implementable; MG-30 depends on the ownership decision; MG-29 needs the live bootstrap. Do NOT touch MG-21/23/25.

**CORRECTION to pause note:** MG-11 (data-pusher Go module-path rename) and MG-12 (azurerm v5 deprecation cleanup) ARE offline-closeable (I missed them). Proceeding with MG-11 now (mechanical rename, notes say safe anytime), then MG-12. MG-10 (infra-security-hardening) may be largely SUPERSEDED by the MG-24 corrective (hardcoded-subscription-id removed, CORS narrowed, managed identity, secrets-in-state disabled) — needs a reconciliation/AC-walk check. So NOT a full pause — MG-11/12/(10-check) remain offline.

**MG-11 CLOSED — main bb72843 (PR#12), 2026-07-21.** data-pusher Go module renamed to github.com/stevebargelt/meatgeekv2/apps/data-pusher (go.mod + all imports + Makefile init-dev); binary/service name meatgeek-pusher legitimately unchanged. Red-wide caught 3 residuals over 2 rounds (Makefile init-dev old path, README roadmap, + a ticket-context nit). go build/vet/test green. **Now: MG-12** (azurerm v5 deprecation cleanup, offline) then MG-10 (infra-security AC-walk vs MG-24 supersession) then MG-13 (Phase 3+ pact, defer).
